import { PolicyHandlersStorage } from './policy-handlers.storage';

describe('PolicyHandlersStorage', () => {
  it('should be defined', () => {
    expect(new PolicyHandlersStorage()).toBeDefined();
  });
});
