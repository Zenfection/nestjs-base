import { FramworkContributorPolicy } from './framwork-contributor.policy';

describe('FramworkContributorPolicy', () => {
  it('should be defined', () => {
    expect(new FramworkContributorPolicy()).toBeDefined();
  });
});
